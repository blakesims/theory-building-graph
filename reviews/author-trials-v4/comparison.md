# V4 regression review

Slow-next-step passes its original three criteria and the new read-output-fidelity regression. It gives a documented single read command, makes no changes, and no longer promises second-hop node bodies. Exact output membership is explicitly unobserved.

Pattern-fidelity passes its three semantic review criteria and the new pattern-versus-trace regression. It keeps the reported predicate as an unratified extraction and does not suggest converting it into event evidence. **The overall trial nevertheless fails:** its batch uses `meta.review_state: reviewed-not-accepted`, which the real CLI rejects. No update was accomplished. Semantic correctness is not an excuse to ignore rejected application.

Both receipts retain exact excerpts, input/raw/rubric hashes, real usage and execution outcome. The two newly added regression criteria are separately marked `criterion_group: regression`; original criteria are unchanged. V1–v3 results remain preserved.

The next packet supplies general documentation of the finite review-currency field and its distinction from pattern standing and review verdict. It keeps the same user turn, graph and rubric. That prospective repair is not a passing result until a new proposal actually validates.
