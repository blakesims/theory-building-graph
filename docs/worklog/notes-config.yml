# Documentation system config — created by /doc:init
# Global skills (/doc:new-note, /doc:sweep) read this file to adapt per-project.
version: 1

# Where transient worklog notes live. The new-note.sh script lives here too.
worklog_dir: docs/worklog

# Where curated reference docs live (agent contract, workflow, schemas).
refs_dir: docs

# flat: all notes in one directory, date-prefixed filenames (project is one day old).
granularity: flat

# No automated summaries yet; revisit when the worklog grows.
summary_cadence: none

integrations: []
